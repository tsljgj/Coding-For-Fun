set<int>::iterator it;
it=s.lower_bound(x);//>=x最小
set<int>::iterator ti=it;
ti--;//不能直接减


stack<int> a;
a.push
a.pop//delete the top
a.top

multiset<int> a;

reverse_iterator//c++ reference cppreference.com

pair<int, int> a[100];
a[i].second;

make_pair(x,y);

s.erase(--s.end());//X s.erase(s.rbegin())

set// 加入/删除/问最大/最小/lowerbound/upperbound
map// 可以映射两个值

s.erase(it++);

//*it 对应值 it对应的是内容

//跳出多重循环：函数 标记变量 goto (int外移) 

结构体：.
结构体指针：-> 

压缩路径： 
